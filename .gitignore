# Derived from public datasets by `python -m credibility.extract`; regenerable,
# and large. The results in out/ are committed so the README renders.
data/
__pycache__/
*.pyc
.pytest_cache/
.venv/

# build artifacts
dist/
build/
*.egg-info/

# strategy, pitch and internal runbooks -- never tracked
docs/_private/
